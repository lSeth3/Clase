import 'package:flutter/material.dart';


class BotonesView extends StatelessWidget {
  const BotonesView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('BOTONES_VIEW')),
    );
  }
}
