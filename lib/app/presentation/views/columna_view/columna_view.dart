import 'package:flutter/material.dart';

class ColumnaView extends StatelessWidget {
  const ColumnaView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('ColumnaView'),),
    );
  }
}
